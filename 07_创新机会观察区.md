# 创新机会观察区

> 生成日期：2026-09-08  
> 来源：[`05_创新证据矩阵.md`](05_创新证据矩阵.md) 与 [`06_局限与反例证据表.md`](06_局限与反例证据表.md)。  
> 本文件不是研究方案或最终选题。每项仅记录已达到最低证据阈值的文献机会，供人工审阅、继续检索或降级。

## 收录规则

每项至少包含两篇独立论文证据，或明确的系统性评测证据；必须同时记录近邻工作和证据不足之处。状态不表达优先级或创新性结论。

## OPP-01：静态优化代理与目标机效应之间的可比较证据缺口

### 已有工作边界

PAPER-46、PAPER-04、PAPER-C06、PAPER-C09、PAPER-38 使用代码尺寸、IR 指令数或 `llvm-mca` 等代理；PAPER-C25、PAPER-C35、PAPER-N15、PAPER-64 则直接测量不同范围的 Runtime。

### 未解决机制

现有证据尚未形成可在不同任务、编译器和目标机之间复用的报告方式，用于说明某个静态代理何时可近似 Runtime、何时会排序错误。

### 近邻工作与重合边界

LIM-01 已包含真实测量和代理偏差的既有做法；机会不等于“使用 Runtime”本身，也不声称不存在任何相关方法。

### 证据清单

- PAPER-03、PAPER-17 提供代理指标与真实性能可能错位的基础设施/传统基线证据。
- PAPER-C25、PAPER-C35 提供真实目标机反馈与预算代价的直接证据。
- PAPER-C09 提供 `llvm-mca` 估计但非真机测量的对照。

### 证据不足处

首轮未系统核验 2024–2026 年专门评估“代理—Runtime 排序相关性”的完整论文集合；不能将此缺口断言为领域空白。

### 待检索关键词

`compiler optimization surrogate reward runtime correlation`；`LLVM IR metric hardware performance ranking`；`compiler autotuning proxy metric calibration`。

### 状态
待人工判断

## OPP-02：验证结论、未判定结果与最终可用输出之间的证据分层

### 已有工作边界

PAPER-12、PAPER-13、PAPER-49、PAPER-C34、PAPER-44 都把验证纳入生成或规则流程；PAPER-14 明确说明 Alive2 的 bounded、unsupported 和 inconclusive 边界。

### 未解决机制

不同论文把测试、编译成功、有界 SMT、Alive2、CBMC 与生产门控混用，但对“拒绝、未判定、接受”如何进入最终成功率的报告方式不统一。

### 近邻工作与重合边界

LIM-02 已汇总多阶段验证路线；这里不把“再调用 Alive2”视作新的机会，只关注跨层证据如何被明确记录和比较。

### 证据清单

- PAPER-12 将 verified/refuted/inconclusive 纳入 RL 奖励，但不覆盖后端。
- PAPER-13 有大量未能验证的向量化输出，且验证扩展增加成本。
- PAPER-C34 显示有界 CBMC 仍可能 false acceptance，Alive2 对复杂 C 函数常超时。
- PAPER-14 给出验证器本身的正式适用边界。

### 证据不足处

尚未审计不同验证器在同一基准、同一生成任务上的交叉覆盖与误拒绝差异；没有足够证据说明某一统一分层已被或未被解决。

### 待检索关键词
`LLM compiler optimization verification inconclusive reporting`；`Alive2 CBMC generated optimization comparison`；`translation validation coverage benchmark`。

### 状态
证据补充中

## OPP-03：真实性能反馈的预算与稳定性可审计比较

### 已有工作边界

PAPER-C25、PAPER-C35、PAPER-10、PAPER-N07、PAPER-64、PAPER-C37 都将候选生成或搜索与真实测量结合，但候选预算、重复次数、LLM 调用成本和搜索空间各不相同。

### 未解决机制

当前首轮证据不能直接判断何种反馈策略“更省”，因为成功率、性能、候选数、运行次数和墙钟时间通常没有作为同一组可比较指标同时呈现。

### 近邻工作与重合边界

LIM-03 已列出限制预算、复用、并行与搜索先验；机会不是简单增加更多搜索轮数或模型调用。

### 证据清单

- PAPER-C35 给出 3 轮与 500 轮搜索的上限差异。
- PAPER-C25 同时报告真实 profiling、模拟反馈和 LLM 查询时间。
- PAPER-N07 采用每程序 25 次执行，明确存在多轮成本与测试过拟合风险。
- PAPER-C37 显示候选编译/宏基准成本和性能目标噪声对生成启发式的影响。

### 证据不足处

尚缺少同一工具链、同一预算下对静态代理、模拟器和真实 Runtime 反馈的统一实验审计；因此不能据此宣称存在未被覆盖的算法机制。

### 待检索关键词
`compiler autotuning measurement budget`；`LLM compiler optimization cost latency evaluation`；`runtime feedback sample efficiency compiler`。

### 状态
待人工判断

## OPP-04：跨平台适用边界的可比报告缺口

### 已有工作边界

PAPER-C35 比较 x86-64 与 ARM64；PAPER-C25 在两代 AMD CPU 上检验；PAPER-64 涉及三种加速器；但 PAPER-13、PAPER-N17、PAPER-C34 等直接生成方法仍局限于单 ISA/平台。

### 未解决机制

现有论文尚未在共同的“程序—优化对象—验证—测量”记录框架内，解释策略、直接变换或规则为何在一个平台有效、在另一个平台失败或未测。

### 近邻工作与重合边界

LIM-04 已说明单平台结论不能外推；机会不等于把实验设备改成 RISC-V 或 GPU。

### 证据清单

- PAPER-C35 直接观察到两种 CPU 的 Pass 行为不同。
- PAPER-C25 提供同 ISA 跨 CPU 的迁移结果。
- PAPER-12、PAPER-44、PAPER-17 明确未验证 IR/规则跨 ISA 的性能。
- PAPER-64 提供加速器平台间结果，但任务限于张量算子。

### 证据不足处

需要补查 MLGO、跨 ISA 优化、LLVM 后端和新近 LLM 编译器工作，才能判断已有平台条件模型已覆盖到何种程度。

### 待检索关键词
`cross architecture compiler optimization transfer`；`platform conditioned LLVM optimization`；`LLM compiler optimization cross ISA evaluation`。

### 状态
观察

## OPP-05：从局部规则/Pass 成功到完整编译流水线收益的证据闭环

### 已有工作边界

PAPER-44 可生成并验证泛化 peephole 规则；PAPER-C36 可生成图编译 Pass；PAPER-C37 可生成启发式；PAPER-17 是传统 SMT 超级优化基线。它们各自测量的对象不同。

### 未解决机制

当前证据不能用单一“规则成功率”或“Pass 正确率”代表最终系统收益，因为局部匹配、语义验证、后端利润和整程序 Runtime 是不同层级的证据。

### 近邻工作与重合边界

LIM-05 已覆盖现有筛选、缓存、PassBench 与宏基准路线；机会不是单纯生成更多规则或 Pass。

### 证据清单

- PAPER-44 的 parser/Alive2/cost model 流程没有完整整程序 Runtime 评测。
- PAPER-17 已给出指令/尺寸收益不保证 Runtime 的传统反例。
- PAPER-C36 记录生成 Pass 的融合边界和硬件代价失败。
- PAPER-C37 在 size 与 performance 任务上得到不同的搜索表现。

### 证据不足处

尚未检索 rule mining、e-graph、MLIR transform dialect 和 compiler pass synthesis 的完整近邻集合；不能把该观察直接称作规则生成方向的研究空白。

### 待检索关键词
`compiler pass generation end-to-end performance evaluation`；`rewrite rule profitability cross backend`；`LLM compiler heuristic synthesis benchmark`。

### 状态
证据补充中

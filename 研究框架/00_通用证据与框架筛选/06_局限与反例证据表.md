# 局限与反例证据表

> 生成日期：2026-09-08  
> 来源：[`05_创新证据矩阵.md`](05_创新证据矩阵.md) 中 33 篇首轮论文级证据。  
> 规则：每项先列论文事实，再给出受限的跨文献推导；“未报告”不等于“方法无效”。

## LIM-01：静态代理指标不能单独代表真实后端性能

### 定义与条件

当方法以 IR 指令数、代码尺寸或 `llvm-mca` 等代理量作为主要奖励/结果时，该量未必与特定目标机的 Runtime、吞吐或能耗同向变化。

### 支持证据（论文事实）

- PAPER-46：仅报告相对 `-Oz` 的代码尺寸与 IR 可编译率，未报告 Runtime（矩阵 §PAPER-46）。
- PAPER-04：以 `instrcount` 为主要奖励；笔记明确指出其不等于真实 CPU 性能（矩阵 §PAPER-04）。
- PAPER-C06、PAPER-C09、PAPER-38：分别以 IR 指令数或 `llvm-mca` cycles 为主要结果，且笔记都记录未使用真实目标机 Runtime（矩阵对应条目）。
- PAPER-03：CompilerGym 明确把 IR/二进制奖励界定为代理，并要求使用者自行验证与真实目标的一致性（矩阵 §PAPER-03）。
- PAPER-17：传统超级优化基线中已观察到更短的指令序列可带来 Runtime 变慢（矩阵 §PAPER-17）。

### 反例或适用边界（论文事实）

- PAPER-C25、PAPER-C35、PAPER-N15、PAPER-64、PAPER-N17 使用真实执行时间，但仍分别局限于特定 CPU、两种 CPU、GCC/C++、三类加速器或 x86 SIMD。
- 因而证据并不支持“静态指标无用”；它支持“静态指标不能在没有目标机核验时被当作真实性能结论”。

### 已有解决路线

真实 compile-run（PAPER-C25、PAPER-C35）、小预算运行时反馈（PAPER-N15）、真实硬件闭环（PAPER-64），以及把代理指标与运行时测量并列报告。

### 事实/推导结论

**论文事实**：多篇工作明确以代理指标评测或承认其偏差，传统 Souper 也给出“更短不必更快”的反例。  
**合理推导**：现有不同论文使用的代理、真机、模拟器和测量预算不统一，因而不能横向把其“优化百分比”视为同一性能含义。

## LIM-02：生成结果的正确性证据存在层级和覆盖边界

### 定义与条件

编译成功、单元测试、差分测试、Alive2/SMT 验证与生产门控提供不同强度的证据；任何一种都需同时报告其语言层级、支持特性和不确定结果。

### 支持证据（论文事实）

- PAPER-12：Alive2 引导 GRPO，但笔记明确记录 Alive2 有界、存在 inconclusive 且不覆盖后端语义（矩阵 §PAPER-12）。
- PAPER-13：约 62% 向量化生成未通过或无法完成验证，且扩展验证提高成本（矩阵 §PAPER-13）。
- PAPER-C34：CBMC 有界通过仍可能 false acceptance，Alive2 对复杂 C 函数常超时（矩阵 §PAPER-C34）。
- PAPER-49、PAPER-40、PAPER-N17：分别使用 Alive2、混合验证、功能测试；笔记都保留了范围/覆盖限制（矩阵对应条目）。
- PAPER-14：Alive2 的工具论文明确限定有界性、未支持特性及验证语义而非性能的边界（矩阵 §PAPER-14）。

### 反例或适用边界（论文事实）

- PAPER-65 的编译、测试、审查和生产 A/B 是更长的工程门控链，但不提供可公开复现的形式证明。
- PAPER-C35 可验证 Pass pipeline 合法并以 rollback 避免性能退化，但没有形式等价证明。

### 已有解决路线

多阶段过滤（PAPER-13、PAPER-C34、PAPER-65）、把验证诊断作为训练/修复反馈（PAPER-12、PAPER-44）及明确保留 `inconclusive` 状态。

### 事实/推导结论

**论文事实**：验证工具与测试都存在可定位边界。  
**合理推导**：当前“验证通过率”只有连同验证层级、覆盖范围和超时/未判定处理方式，才具有跨论文解释意义。

## LIM-03：反馈越接近真实性能，通常越受预算、噪声与系统成本约束

### 定义与条件

基于真实 compile-run、profiling 或宏基准的循环可直接评价目标，但会带来候选数、测量次数、LLM API 延迟或搜索时间的约束。

### 支持证据（论文事实）

- PAPER-C25：真实 cycles 与模拟信息并用；笔记记录 LLM 集成对部分函数退化，且热点函数最优不等于整程序最优（矩阵 §PAPER-C25）。
- PAPER-C35：三轮预算下有收益，但 500 轮 OpenTuner 仍可更高，说明预算影响结果上限（矩阵 §PAPER-C35）。
- PAPER-10、PAPER-N07：分别把样本效率/25 次重复执行作为核心成本，指出 LLM 调用与测量成本（矩阵对应条目）。
- PAPER-64、PAPER-C37：真实硬件/宏基准闭环有效，但多轮测量或候选编译成本高（矩阵对应条目）。

### 反例或适用边界（论文事实）

- PAPER-C40 显示在输入特征分析任务中，编译器引导归约可显著降低分析成本；它不是直接代码变换任务，不能直接外推为所有优化搜索的成本结论。

### 已有解决路线

限制候选预算（PAPER-C35）、并行 LLM 查询与 profiling（PAPER-C25）、用搜索/复用减少重复评测（PAPER-10、PAPER-64）、及先验知识/受约束动作空间（PAPER-C06、PAPER-N15）。

### 事实/推导结论

**论文事实**：真实反馈和搜索预算之间存在可见取舍，且高预算搜索在部分工作中可继续改善。  
**合理推导**：应把性能、候选数、测量次数、LLM 调用和总墙钟时间分开报告，否则“更高效”无法比较。

## LIM-04：跨平台适用边界尚未被统一刻画

### 定义与条件

同一代码变换、Pass 策略或规则在不同 ISA、编译器版本、微架构和加速器上是否保留正确性/收益，不能由单平台结果直接推出。

### 支持证据（论文事实）

- PAPER-46、PAPER-47、PAPER-04、PAPER-12 都只在 LLVM IR/静态或单环境层面验证，笔记明确记录未做多 ISA/后端收益验证。
- PAPER-C35 真实比较 x86-64 与 ARM64，并显示 pipeline 行为不同，但未覆盖 RISC-V（矩阵 §PAPER-C35）。
- PAPER-13、PAPER-N17 只覆盖 x86 SIMD；PAPER-C34 只给 AArch64；PAPER-64 覆盖三类加速器但仍局限于张量算子。
- PAPER-44 和 PAPER-17 都明确未系统验证规则/局部重写的跨 ISA 性能。

### 反例或适用边界（论文事实）

- PAPER-C25 在两代 AMD CPU 上报告迁移结果，说明同 ISA 内迁移可以实证，但并不构成跨 ISA 证据。

### 已有解决路线

在多目标平台分别测量（PAPER-C35、PAPER-64），并在论文中把平台、编译器版本、基准和测量方式作为方法条件报告。

### 事实/推导结论

**论文事实**：多数学术评测仍是单一平台；少数多平台工作覆盖范围彼此不同。  
**合理推导**：目前不能把“跨平台泛化”当作统一已解决能力，只能针对明确的平台集合讨论。

## LIM-05：可复用规则/Pass 的局部正确或局部收益，不等于全流程收益

### 定义与条件

生成 rewrite rule、compiler pass 或启发式时，局部匹配、语义正确、局部速度和进入完整编译管线后的最终性能是不同命题。

### 支持证据（论文事实）

- PAPER-44：用 parser、Alive2 和 cost model 筛规则，但未在完整编译流水线测全程序 Runtime（矩阵 §PAPER-44）。
- PAPER-17：局部 SMT 合成能减二进制大小，但某些设置 Runtime 变慢（矩阵 §PAPER-17）。
- PAPER-C36：PassBench 显示模型整体尚未达到 G-Mean≥1，失败与融合边界及硬件代价有关（矩阵 §PAPER-C36）。
- PAPER-C37：启发式生成在 size 任务有效，但性能任务的独立搜索一度未超过 0%，显示目标/初始化敏感（矩阵 §PAPER-C37）。

### 反例或适用边界（论文事实）

- PAPER-C37 的真实宏基准大小结果说明生成组件可以被完整评估，但其内部数据和环境限制复现。

### 已有解决路线

多层规则筛选（PAPER-44）、缓存并在真实构建中测量（PAPER-17）、基准和失败分类（PAPER-C36）、宏基准与黑盒调参（PAPER-C37）。

### 事实/推导结论

**论文事实**：生成组件的局部评测与完整系统性能可产生不同结论。  
**合理推导**：规则/Pass 的报告需要区分“可生成”“可验证”“可集成”“最终有效”四类证据，不能只用其中之一代表全部。

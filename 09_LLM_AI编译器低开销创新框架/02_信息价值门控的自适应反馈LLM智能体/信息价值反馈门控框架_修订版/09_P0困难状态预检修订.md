# P0 困难状态预检修订

第一次仅工具准备运行 `p0-hard-prepare-20260720-001` 未调用模型。检查发现 Remark 状态中的 Clang remark 指向测试驱动的随机数据生成循环，而非目标函数，因此该准备运行无效并归档，不进入正式分析。

正式运行前固定修订如下：

- Compile、Test、IR 反馈生成保持不变；
- Remark 使用只包含目标函数的独立 C 翻译单元；
- 目标函数添加 `used,noinline`，防止未调用函数在优化前消失；
- 同时开启 loop-vectorize 的 passed、analysis、missed remark；
- 反馈必须明确来自目标函数翻译单元且非空，否则禁止开始正式运行。

该修订发生在任何困难状态 API 调用之前，不使用模型结果调参。其他预注册门槛不变。

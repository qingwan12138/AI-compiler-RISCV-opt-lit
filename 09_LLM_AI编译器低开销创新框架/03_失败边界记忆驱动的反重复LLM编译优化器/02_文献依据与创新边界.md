# 失败边界记忆框架：文献依据与创新边界

## 1. 结论

成功案例检索、历史模式库和当前任务内的错误拒绝均已有代表工作。候选空间是“跨任务负证据的适用边界和失效机制”，但直接文献证据比反馈门控方向弱。

## 2. 主要文献证据

| 文献 | 【论文事实】已覆盖内容 | 本框架必须区别的部分 |
|---|---|---|
| Code the Transforms（2024） | 用反例修正生成的变换器 | 反例用于规则修正，不是跨任务失败记忆 |
| SBLLM（2025） | 检索优化模式并进行多轮候选搜索 | 成功模式检索是强基线 |
| GraphGlue（2025） | SRS 拒绝当前失败策略并重新生成 | 当前任务拒绝已覆盖 |
| ECO（2025） | 从历史提交挖掘已验证性能模式 | 历史模式库和生产验证已覆盖 |
| Code-Optimise（2025） | 正确/错误和快/慢形成偏好数据 | 失败样本用于训练已有先例 |
| IntOpt（2026） | 知识库和失败闭环，避免重复策略 | 知识库与失败记录不是创新 |
| SemOpt（2026） | 规则分析与检索指导优化 | 规则/RAG 组合已有近邻 |

## 3. 研究空白推导

【论文事实】现有系统会丢弃、修复或训练失败样本，也会检索成功模式。

【合理推导】一个失败动作通常只在特定程序结构、参数、编译器和目标条件下失败。如果缺少适用边界，失败记忆会从“减少重复”变成“错误禁止探索”。现有评价也很少直接报告负记忆误拦截率。

【候选创新】将失败转为带适用、反例和失效条件的负约束，并通过抽样执行被拦截动作测量误拦截。

## 4. 不能声称的内容

- 首次使用 RAG；
- 首次构建优化知识库；
- 首次保存失败日志；
- 首次使用反例；
- 首次拒绝错误策略；
- 首次从历史提交或轨迹提取模式；
- 首次使用经验回放或负样本。

## 5. 最近邻比较

| 方法 | 记忆对象 | 是否有适用条件 | 是否有失效条件 | 是否测误拦截 |
|---|---|---|---|---|
| 成功案例 RAG | 成功代码/模式 | 相似度隐式决定 | 通常无 | 通常无 |
| 失败黑名单 | 动作或错误字符串 | 无或很弱 | 人工删除 | 很少 |
| 当前任务拒绝 | 当前策略 | 当前上下文 | 重新生成即结束 | 不适用 |
| 失败边界记忆 | 失败动作与证据 | 工具可检查 | 反例和误拦截驱动 | 核心指标 |

## 6. 必须完成的实验

1. 与无记忆比较重复失败率；
2. 与成功案例 RAG 比较固定预算有效优化率；
3. 与无条件失败黑名单比较误拦截率；
4. 去掉反例条件；
5. 去掉编译器/目标作用域；
6. 抽样执行被 BLOCK 的动作，估计真实 false block；
7. 在未见程序家族上验证迁移。

## 7. 证据风险

当前文献证据不足，需要继续查找相关研究，不能直接声称这是创新。特别需要检索程序修复、持续学习和 Agent memory 中的负经验边界。

关键词：

- `negative experience memory code agent`
- `failure-conditioned retrieval program repair`
- `counterexample invalidation agent memory`
- `conditional blacklist compiler optimization`
- `failure boundary continual code optimization`

## 8. 推荐等级

- 新颖性：4/5（未完成系统检索，置信度中）；
- 文献证据：3/5；
- 可实现性：4/5；
- 实验可验证性：4/5；
- 时间开销：中；
- 定位：独立备选或第二阶段扩展。

## 9. 三项创新的证据边界

| 创新层级 | 【论文事实】已有覆盖 | 【合理推导】研究空白 | 【候选创新】可保留主张 | 证据等级 |
|---|---|---|---|---|
| 核心创新 | SBLLM、GraphGlue 等支持历史模式或检索，普通 RAG 可复用案例 | 相似失败何时适用、何时失效仍容易由文本相似度代替 | 带适用、反例和工具链范围的负证据表示 | 中 |
| 支撑创新一 | 黑名单和规则门控可直接允许或禁止动作 | 部分条件满足时缺少能够补充证据的中间状态 | ALLOW/WARN_AND_MEASURE/BLOCK 三态门控 | 中 |
| 支撑创新二 | 在线计数和置信度更新是常见机制 | 被禁止动作没有结果，错误记忆可能无法被推翻 | 误拦截反事实审计和反例驱动失效更新 | 中高 |

### 9.1 不能声称的内容

- 建数据库、保存失败日志、使用 RAG 或设置黑名单本身不是创新；
- 三态枚举若没有适用和反例条件驱动，只是工程状态机；
- 置信度更新若不审计被拦截动作，不能主张解决选择偏差；
- 当前文献证据不足，需要继续查找相关研究，不能直接声称这是确定创新。

### 9.2 分创新证伪

1. 核心创新：成功案例 RAG 或无条件失败记忆达到同等结果；
2. 支撑创新一：完整三态门控不能降低 FalseBlockRate 或补测没有收益；
3. 支撑创新二：误拦截审计不能恢复有效动作，或审计成本高于避免失败的收益。

# 近邻工作与创新边界表

> 生成日期：2026-09-08  
> 范围：仅审计 [`07_创新机会观察区.md`](07_创新机会观察区.md) 的 OPP-01 至 OPP-05。  
> 使用方式：本表不宣布研究空白；它记录最近工作的已覆盖范围、仍需人工核验的条件与潜在碰撞。表中“论文事实”来自现有阅读笔记，“分析推断”仅用于界定后续审阅边界。

## 阅读规则

- `PAPER-` 编号的标题、出版信息与原笔记以 [`00_三大类分类索引_v2.md`](00_三大类分类索引_v2.md) 为准。
- “仍未覆盖”表示本库现有证据没有显示该条件已被统一处理，不表示领域中绝对不存在相关工作。
- 重合风险越高，越不能仅凭当前机会名称主张独立框架。

## OPP-01：静态优化代理与目标机效应之间的可比较证据缺口——近邻碰撞审计

### 近邻论文

| Paper ID | 已覆盖任务/输出 | 已覆盖反馈与验证 | 仍未覆盖的条件 | 关系性质 |
|---|---|---|---|---|
| PAPER-C25 | 为 LLVM Pass 搜索输出策略/候选；在真实 CPU 上评测 | 同时记录 profiling、模拟反馈与查询时间 | 未见在共同任务中校准静态代理与 Runtime 排序误差 | 论文事实 + 分析推断 |
| PAPER-C35 | 为不同程序选择优化序列 | 使用真实 Runtime 比较多轮搜索 | 未见将代理失真作为跨任务、跨目标机的可比较报告对象 | 论文事实 + 分析推断 |
| PAPER-C09 | 估计 LLVM 代码的微架构吞吐/延迟 | `llvm-mca` 静态估计，不是目标机 Runtime | 未给出与端到端真实运行排序的一般对应条件 | 论文事实 + 分析推断 |
| PAPER-03 | 提供可用代理奖励的 LLVM 优化环境 | 静态观测与运行/尺寸目标可选 | 不直接解决代理与目标机效应的可迁移对应 | 论文事实 + 分析推断 |
| PAPER-17 | 生成局部更优的 LLVM IR 片段 | 使用成本/指令等局部指标 | 局部收益到整程序 Runtime 的对应未由该工作保证 | 论文事实 + 分析推断 |

### 尚未覆盖的条件

需要的是同一记录单位同时保存：程序与变换、静态代理、目标平台、真实 Runtime 与排序/误差关系；只报告其中任一项不构成可比较证据。

### 重合风险

中。直接测量 Runtime、使用 `llvm-mca` 或使用代理奖励都已有近邻。不能把“加入真实运行时间”或“换一个代理”单独称为独立框架。

### 事实/推断

- 论文事实：PAPER-C25、PAPER-C35 使用真实测量；PAPER-C09 使用静态微架构估计；PAPER-03、PAPER-17 提供代理/局部收益语境。
- 分析推断：当前库未显示它们在同一通用格式中报告代理—Runtime 排序关系；这需要回到全文与补充近年文献复核。

### 人工复核重点

核验近邻论文是否已有 held-out 程序上的相关性、校准或 rank-correlation 报告；若存在，应把本机会降为已有评测实践而非保留观察。

## OPP-02：验证结论、未判定结果与最终可用输出之间的证据分层——近邻碰撞审计

### 近邻论文

| Paper ID | 已覆盖任务/输出 | 已覆盖反馈与验证 | 仍未覆盖的条件 | 关系性质 |
|---|---|---|---|---|
| PAPER-12 | 直接输出优化 LLVM IR | Alive2 的 verified/refuted/inconclusive 进入反馈 | 未统一记录后端可用性与最终交付状态 | 论文事实 + 分析推断 |
| PAPER-13 | 直接输出 SIMD/向量化代码 | 测试与 Alive2；存在未验证输出 | 未将不同验证器的拒绝/未判定纳入共同可比口径 | 论文事实 + 分析推断 |
| PAPER-C34 | 直接生成/修复编译优化代码 | 编译、CBMC、Alive2 等多层检查 | 有界检查和超时边界未等同于统一证据等级 | 论文事实 + 分析推断 |
| PAPER-14 | 提供 LLVM 优化等价验证基础设施 | 明确 bounded、unsupported、inconclusive 边界 | 不负责生成系统最终“可用输出”的报告协议 | 论文事实 + 分析推断 |
| PAPER-44 | 输出可复用 peephole 规则 | parser、Alive2 与成本筛选 | 规则级验证不自动说明整流水线接受/拒绝状态 | 论文事实 + 分析推断 |

### 尚未覆盖的条件

需要将“编译/测试/形式验证/未判定/超时/后端可执行”明确区分并可追溯到最终成功率；单独追加某个验证器不满足这一条件。

### 重合风险

高。多阶段验证、验证反馈和未判定处理均已有直接近邻；仅提出“分层验证”会与 PAPER-12、PAPER-13、PAPER-C34 发生高重合。

### 事实/推断

- 论文事实：上述论文均采用或讨论不同验证层，PAPER-14 直接刻画 Alive2 的适用边界。
- 分析推断：跨验证器的统一报告口径是否已有完整实现，不能由当前首轮笔记确定。

### 人工复核重点

重点比对论文的 success-rate 分母：是否包含未判定、超时、编译失败与后端不可执行样本；若已有统一分母，应停止保留该机会。

## OPP-03：真实性能反馈的预算与稳定性可审计比较——近邻碰撞审计

### 近邻论文

| Paper ID | 已覆盖任务/输出 | 已覆盖反馈与验证 | 仍未覆盖的条件 | 关系性质 |
|---|---|---|---|---|
| PAPER-C25 | 输出 Pass 搜索策略 | 真实 profiling、模拟反馈和查询时间 | 未见统一披露候选数、重复次数、墙钟与预算上限 | 论文事实 + 分析推断 |
| PAPER-C35 | 输出优化序列 | 比较少量与大量搜索轮数的 Runtime | 未见与其他反馈类型共享相同预算账本 | 论文事实 + 分析推断 |
| PAPER-10 | 输出优化决策/策略 | 使用编译器交互反馈 | 反馈成本与稳定性字段不一定能跨论文直接比较 | 论文事实 + 分析推断 |
| PAPER-N07 | 直接输出优化程序候选 | 每程序多次执行并有测试过拟合风险 | 未形成与静态代理、模拟器并列的成本—稳定性口径 | 论文事实 + 分析推断 |
| PAPER-C37 | 输出可复用编译器启发式 | 候选编译、宏基准和性能目标噪声 | 未见统一预算下的反馈方案比较 | 论文事实 + 分析推断 |

### 尚未覆盖的条件

可审计比较至少需要同时保留候选数量、编译次数、执行次数、重复测量、LLM 调用/墙钟成本和性能方差；只给 speedup 或迭代轮数不足。

### 重合风险

中。预算限制、搜索效率和运行噪声都已有工作；不能把“少测几次”或“多轮 RL”作为独立机制。

### 事实/推断

- 论文事实：近邻分别报告真实测量、搜索轮数、多次执行或宏基准成本。
- 分析推断：这些字段未必按同一单位共同报告；该判断需核对原文表格与 artifact。

### 人工复核重点

对每篇近邻补录实际运行次数、warm-up、方差、总墙钟与模型调用成本；若多数论文已有统一成本表，本机会应降级。

## OPP-04：跨平台适用边界的可比报告缺口——近邻碰撞审计

### 近邻论文

| Paper ID | 已覆盖任务/输出 | 已覆盖反馈与验证 | 仍未覆盖的条件 | 关系性质 |
|---|---|---|---|---|
| PAPER-C35 | 为程序输出平台相关的 Pass 序列 | x86-64 与 ARM64 Runtime 比较 | 未在统一框架中解释何时策略应迁移、何时应改变 | 论文事实 + 分析推断 |
| PAPER-C25 | 输出 LLVM 优化策略 | 两代 AMD CPU 的迁移测试 | 同 ISA 跨 CPU 不等同于跨 ISA/跨后端边界 | 论文事实 + 分析推断 |
| PAPER-64 | 输出加速器算子/调度优化 | 多加速器结果 | 任务集中于张量算子，未覆盖通用 LLVM 变换 | 论文事实 + 分析推断 |
| PAPER-13 | 输出向量化代码 | 主要在有限 ISA/平台下验证 | 未提供跨平台收益归因 | 论文事实 + 分析推断 |
| PAPER-N17 | 输出低层代码/迁移结果 | 单一或有限目标环境评估 | 跨平台泛化条件未被系统拆解 | 论文事实 + 分析推断 |

### 尚未覆盖的条件

需要对相同程序、优化对象、验证状态和测量协议记录平台差异，并标明决策是否改变及其性能后果；仅增加一个 RISC-V、GPU 或 ARM 实验不满足该条件。

### 重合风险

中。跨平台测试与平台相关选择已有近邻；不能把“多测一个平台”称为框架增量。

### 事实/推断

- 论文事实：PAPER-C35、PAPER-C25、PAPER-64 给出不同程度的平台比较；PAPER-13、PAPER-N17 的平台范围更有限。
- 分析推断：现有工作是否已定义统一边界报告格式，需要补查跨 ISA、MLGO 和后端文献。

### 人工复核重点

检查各近邻是否保持相同 benchmark、编译选项、验证和测量协议；若不一致，不能将速度差直接解释为平台条件效应。

## OPP-05：从局部规则/Pass 成功到完整编译流水线收益的证据闭环——近邻碰撞审计

### 近邻论文

| Paper ID | 已覆盖任务/输出 | 已覆盖反馈与验证 | 仍未覆盖的条件 | 关系性质 |
|---|---|---|---|---|
| PAPER-44 | 输出可复用 peephole 规则 | parser、Alive2、成本模型筛选 | 规则局部收益与整程序 Runtime 的归因链不完整 | 论文事实 + 分析推断 |
| PAPER-C36 | 输出图编译 Pass | 生成、融合边界和硬件代价评测 | Pass 成功不自动连接到通用后端流水线收益 | 论文事实 + 分析推断 |
| PAPER-C37 | 输出编译器启发式 | 宏基准、尺寸与性能搜索 | 不同目标下的局部/端到端收益不能直接合并 | 论文事实 + 分析推断 |
| PAPER-17 | 输出局部超级优化结果 | SMT 与局部成本/指令收益 | 已提供局部静态收益不保证 Runtime 的反例基础 | 论文事实 + 分析推断 |
| PAPER-14 | 提供 LLVM 等价验证工具 | 语义等价验证 | 语义正确不评价后端利润或整程序效果 | 论文事实 + 分析推断 |

### 尚未覆盖的条件

完整闭环须能追溯规则/Pass 的适用前提、语义验证状态、后端收益和整程序 Runtime；只验证规则正确、只报告匹配数或只报告局部成本均不足。

### 重合风险

中。规则生成、Pass 生成、成本筛选与宏基准均已有近邻；不能把“端到端评测”本身称作新机制。

### 事实/推断

- 论文事实：近邻分别覆盖规则/Pass/启发式生成、形式验证、局部成本或宏基准。
- 分析推断：当前库未显示四层证据被同一可追溯单位连通；需要补查 rule mining、e-graph 和 MLIR transform 近邻。

### 人工复核重点

核验每篇近邻是否把局部规则应用统计与最终二进制/整程序 Runtime 一一关联；如已有完整归因链，应将本机会降级为近邻重合高。
